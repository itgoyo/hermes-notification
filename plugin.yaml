name: hermes-notification
version: 2.0.0
description: "macOS system notification when Hermes finishes a response. Clickable: opens web-ui session in browser if running, or focuses terminal window in CLI mode. Requires terminal-notifier (brew install terminal-notifier)."
author: hermes
requires_env: []
hooks:
  - post_llm_call
